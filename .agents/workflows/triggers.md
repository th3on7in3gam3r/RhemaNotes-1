# RhemaNotes Workflow Triggers

Use the following commands to trigger automated study workflow execution:

- `/transcribe-audio [file-path]`: Trigger audio → text pipeline
- `/generate-study-guide [transcript]`: Create full sermon summary artifact
- `/find-cross-references [passage]`: Lookup supporting scriptures via Bible API
- `/export-pdf [artifact]`: Convert markdown to PDF for sharing
